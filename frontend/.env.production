# URL del backend en producción (Render). No es un secreto: es un endpoint público.
# Vite carga este archivo automáticamente al hacer `vite build` (modo production).
# En desarrollo (`npm run dev`) NO se usa, así que sigue valiendo el localhost de config.json.
VITE_API_URL=https://repuestoszamora-website.onrender.com
